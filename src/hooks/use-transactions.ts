/**
 * Backward-compatible wrapper around useTransactionsQuery.
 * Existing components that import from this file continue to work unchanged.
 */
export { useTransactionsQuery as useTransactionsFetch } from "./use-transactions-query";
